# Data

This directory is reserved for local runtime data and deliberately curated, non-sensitive test fixtures.

- Runtime data is ignored by Git by default.
- Never store credentials, customer data, or raw production output here.
- Commit fixtures only under `data/fixtures/` and document their origin, license, and sanitization.
- Current datasets and schemas: `UNKNOWN`.
